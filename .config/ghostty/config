theme = catppuccin-mocha
background-opacity = 0.95
background-blur-radius = 20
window-decoration = false

# Run tmux attach on startup, create session if none exists
command = /opt/homebrew/bin/tmux new-session -A
